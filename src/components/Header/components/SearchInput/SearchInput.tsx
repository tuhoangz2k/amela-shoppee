import React from 'react';
import { SearchInputWrap } from './SearchInput.styled';
type Props = {};

const SearchInput = (props: Props) => {
  return <SearchInputWrap>SearchInput</SearchInputWrap>;
};

export default SearchInput;
