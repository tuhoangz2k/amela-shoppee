export interface IProductList {
  id?: number | string;
  imageLink: string;
  name: string;
  price: number;
  to: string;
}
export interface IProduct {
  imageUrl: string;
  title: string;
  price: number;
  description: string;
}
